# Refresh the Metadata for a Collection
This only affects metadata in Looks Rare

## To Prepare to Run
First update the "OWNER" and "COLLECTION" variables in bot.py then execute `python bot.py` in a command window.